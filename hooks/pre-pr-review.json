{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'gh pr create' && echo 'Before creating this PR, run self-review on the current diff and report findings. Only proceed with gh pr create if there are no 🔴 security issues.' || true"
          }
        ]
      }
    ]
  }
}
